---
layout: default
title: Blog post
---

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
      type: "posts"
    values:
      layout: "default"

# Build settings
remote_theme: jekyll/minima
